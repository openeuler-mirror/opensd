---
customize_etc_hosts: True

change_selinux: True

selinux_state: "disabled"

pkg_install:
 - git
 - python-setuptools
 - sudo
 - python3-libselinux
 - selinux-policy
 - python-pip
 - python-lxml
 - python-openstackclient


pkg_removals: []
